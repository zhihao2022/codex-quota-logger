[project]
name = "codex-quota-logger"
version = "0.1.0"
description = "Minimal local Codex quota history logger and dashboard"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false